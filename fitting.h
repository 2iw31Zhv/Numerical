#pragma once

void fitting2(double * a,
	double * b,
	double * c,
	int n,
	const double * x,
	const double * y);

void fittingexp(double * a,
	double * b,
	int n,
	const double * x,
	const double * y);